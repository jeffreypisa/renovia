{% block html_head_container %}
	{% block head %}
		{% include 'html-header.twig' %}
	{% endblock %}
{% endblock %}

	<body class="{{ body_class }} {% if (fn('is_page_template') == 'projecten') or (fn('is_archive')) %}bg-bluedark{% endif %}" data-template="base.twig" data-scroll-container>	
		{% include 'mobilemenu.twig' %}
		
		{% block header %}
			{% include 'header.twig' %}
		{% endblock %}
		
		<main>
			{% if post.title %}
				<h1 class="d-none">{{ title }}</h1>
			{% endif %}
			{% block content %}
				Sorry, no content
			{% endblock %}
		</main>
		
		{% block footer %}
			{% include 'footer.twig' %}
			{{ function('wp_footer') }}
		{% endblock %}
	</body>
</html>
