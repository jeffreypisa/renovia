<section class="mod-contact content-styling" data-scroll-section>
	<div class="container">
		<div class="row anime-fadein">
			<div class="col-md-6 col-lg-5 offset-lg-1">
				{% if item.titel %}
					<h2 class="h1 text-green"> {{ item.titel }} </h2>
				{% endif %}
				{{ item.tekst }}
			</div>
			<div class="col-md-6 col-lg-5">
				{{ item.formulier }}
			</div>
		</div>
	</div>
</section>